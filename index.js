// Divide f(x) expression : divides 2 numbers 

const divide = function(dividend = 2000, divisor = 100){

    const  quotient = dividend / divisor; 

    return quotient;
}

// Squaring f(x), defined using an arrow f(x)
const square = number => number * number ;

// Adding f(x), defined using an arrow f(x)

const add = (num1, num2) => num1 + num2; 